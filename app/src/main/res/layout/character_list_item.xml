<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/character_list_item_layout"
    android:layout_width="match_parent"
    android:layout_height="125dp"
    android:layout_margin="10dp"
    android:padding="5dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/image_character_list_item"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:civ_border_width="4dp"
        app:civ_border_color="#000"/>

    <TextView
        android:id="@+id/name_character_list_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/image_character_list_item"
        android:text="Name"
        android:textColor="#000"
        android:textSize="16sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/description_character_list_item"
        android:layout_width="225dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/name_character_list_item"
        app:layout_constraintLeft_toRightOf="@+id/image_character_list_item"
        android:layout_marginLeft="10dp"
        android:text="Description"
        android:textColor="#000"
        android:textSize="14sp"/>

    <TextView
        android:id="@+id/categories_character_list_item"
        android:layout_width="225dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/description_character_list_item"
        app:layout_constraintLeft_toRightOf="@+id/image_character_list_item"
        android:layout_marginTop="7dp"
        android:layout_marginLeft="2dp"
        android:text="Categories"
        android:textColor="#000"
        android:textSize="14sp"
        android:textStyle="italic"/>

</android.support.constraint.ConstraintLayout>